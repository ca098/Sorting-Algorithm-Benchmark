#include <stdlib.h>
#include <stdio.h>

void shellSort() {

  int inner, outer;
  int usersValue;
  int intervalAmount = 1;
  int elements
  int i = 0;

  while(intervalAmount <= elements/3) {
     intervalAmount = intervalAmount*3 +1;
  }
}
